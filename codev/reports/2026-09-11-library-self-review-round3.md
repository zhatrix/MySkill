# codev 全库自审：第三轮

审前版本为 `50db136`。延续前两轮对共享库、测试和调用说明的检查，本轮重点核对故障恢复、并发读写及清理逻辑的组合行为。确认并修复五类问题；新增 19 项回归在审前版本上为 `pass=1 fail=18`，修复后 bash、zsh 全量均为 `pass=234 fail=0`。

## 发现与修复

| ID | 级别 | 复现与影响 | 修复与验证 |
|---|---|---|---|
| r3-01 | P2 | metrics 目标被目录占用时，正文已经解包、原始 usage 消失；移除故障后再次 report 命中状态缓存，仍无法恢复用量，且两次都宣称完成 | 按 0600 原子保存计量与状态，最后替换正文；保存失败保留原始 JSON 并明确提示重试。验证故障后原文不变，重试恢复 `tokens 5` 和 `0.250 USD`，计量文件权限不扩大 |
| r3-02 | P1 | Python 不可用或异常退出时，结构化 `is_error=true, result=PASS` 被当作普通成功正文 | 区分普通文本与无法验证的结果，后者返回 12 并由 report 标为 error；已知错误 10/11 及超时优先级继续保留。验证缺失/损坏解析器均不能放行，正常 `[P1]` 开头的文本仍可呈现 |
| r3-03 | P1 | 新判断写到最后一列前暂停，此时半行恰好像旧 12 列记录；回放未加读锁，把新立场分进旧任务组，原任务仍按旧一致意见宣称可执行。账本路径为目录时，四个读取入口还会将读取故障误当空账本 | 共享锁内复制完整账本，随后统计或裁决，用完删除私有副本；读取失败返回非零。回归让写者暂停后再回放，确认等待完整新立场并报告分歧；四个读取入口均验证错误传播 |
| r3-04 | P1 | GC 只比较路径字符串；当前会话通过符号链接、尾斜杠表示，或嵌套在待清理父目录中时，可能被当成过期目录删除 | 先解析物理路径，两类清理均保护当前会话及其父目录；定位当前会话失败时停止清理。验证三种路径边界，确认无关过期目录仍被正常删除 |
| r3-05 | P2 | 意见写入接受空主体或问题编号，随后该文件被自己的回放校验判为损坏；纯空格身份也可成为无实际主体的判断票 | 写入前拒绝空值与纯空白的 agent/issue，回放采用相同非空白要求，保持实参数量、角色枚举及旧格式兼容。四种拒收测试确认没有追加坏记录 |

## 最终核对

- bash、zsh 各 234 项通过；既有结果状态、母本签名与并发、隔离权限、Git 提交恢复、归档和意见一致性测试继续通过
- bash/zsh 语法检查及 `git diff --check` 通过；zsh 的沙箱 nice 提示未阻止子任务，相关并发断言全部通过
- 最终源码差异、README、接口说明及测试范围已核对，实际变更逐项记录在 [CHANGELOG](../CHANGELOG.md)
- 本轮只使用临时仓库与账本，没有改写真实历史或调用外部模型；旧库已经丢掉的原始 usage 无法据此补回
- 共享锁要求读写双方采用新库；取得副本后发生的后续追加不属于该次读取，修复执行前仍须按协议核对任务状态
- 完整 v2 角色选择、固定评审快照、最终文本验收及真实外部 agent 流程仍未完成；本轮库回归不能替代这些协议验收
