# Changelog

本文件记录 codev 自身的实际变更；目标项目的 spec / 计划回流历史保存在对应项目。记录从本次接入起建立，
更早版本可通过 Git 历史及 `reports/2026-09-10-ntms-usage-audit.md` 追溯，不在此虚构历史版本。

## [Unreleased]

### 🚀 Added

- 2026-09-10 [C001] `SKILL.md`、`references/synthesis.md` 与 `references/changelog.md` 的 prowler-changelog 调用协议，每项实际变更记录对象、阶段、关联发现及验证状态，修复提交显式包含日志；skill 结构及文案一致性已核对，现有库测试 bash/zsh 各 137 项通过，实际编排器调用待真机验证
- 2026-09-10 [C002] `reports/2026-09-10-ntms-usage-audit.md` 的 ntms 使用审计，核对 9 月 5 日至 10 日的调用记录、缓存统计口径及回流缺陷；引用路径和统计加总已核对
- 2026-09-10 [C003] `specs/2026-09-10-review-protocol-v2.md` 的评审协议草案，定义角色选择、一致判断、固定版本、完整差异交付、修复后验收、旧账本兼容及自动本地提交；文档自审、章节与本地引用检查完成，人数上限作用范围待确认，协议实现尚未开始
- 2026-09-10 [C004] 根目录 `.gitignore` 排除本机 `prowler-changelog` skill 链接，避免将本地安装路径纳入版本控制；已核对 Git 忽略结果
- 2026-09-11 [C006] 意见与判断记录：`bin/codev-lib.sh` 新增 `codev_opinion_add` / `codev_opinions` 与 `CODEV_OPINIONS`（默认 `~/.local/state/codev/opinions.tsv`，12 列），一个主体对一个问题的一条立场一行（role 评审/判断/自审/复核，stance 提出/采纳/驳回/存疑/未返回，三列当场校验枚举）；`codev_opinions [问题ID]` 按问题回放并判出 一致采纳同一修法／一致驳回／修法不同(disputed)／未达成一致，缺席须显式记 `未返回`；同步 `specs/2026-09-10-review-protocol-v2.md` §8.3、`SKILL.md`、`README.md`、`references/agents.md`、`references/synthesis.md`、`TESTING.md`，新增 16 项库测试，bash/zsh 各 153 项通过
- 2026-09-11 [C024] `reports/2026-09-11-library-self-review.md` 的全库自审记录，覆盖共享库全部函数、测试脚本和主要调用说明，列出六类已修复问题、复现及协议迁移边界；本批新增 21 项回归，bash/zsh 各 197 项通过，真实外部模型流程未运行
- 2026-09-11 [C030] `reports/2026-09-11-library-self-review-round2.md` 的第二轮全库自审记录，列出结构化状态、精确计量、并发持久化、损坏意见与历史归档五类修复；新增 18 项回归在旧库上 16 项失败，最终 bash/zsh 各 215 项通过，真实外部模型流程未运行
- 2026-09-11 [C037] `reports/2026-09-11-library-self-review-round4.md` 的第四轮全库自审记录，覆盖批量调用下的失败可见性与上一轮仅在文档层处理的缺陷，列出四类修复及自审过程中自己踩的三个坑；新增 10 项断言在审前版本上 9 项失败，最终 bash/zsh 各 247 项通过
- 2026-09-11 [C036] `reports/2026-09-11-library-self-review-round3.md` 的第三轮全库自审记录，列出故障恢复、解析器错误、并发读取、GC 与意见身份五类修复；新增 19 项回归在审前版本上 18 项失败，最终 bash/zsh 各 234 项通过，真实外部模型流程未运行

### 🔄 Changed

- 2026-09-25 [C043 / 使用审计回流] `reports/2026-09-25-usage-audit.md`：核对 9 月 11 日至 25 日的调用账本（43 个会话 302 次调用）、发现台账（1642 行）、意见记录（1670 行）、14 个真实使用会话的转录与 claude-mem 记忆，按 环境/CLI 故障/库缺陷/编排错误/用户摩擦 五类列出 34 个问题及各自回流状态；统计脚本与原始报告在会话 scratchpad
- 2026-09-25 [C044 / 使用审计回流] `bin/codev-lib.sh` 的 `codev_classify` / `codev_match_class` / `codev_err_lines`：503/529/overloaded/"experiencing high demand" 归 `quota`（额度/限流/过载），`API Error …`、`status: 5xx`、`"code": 5xx` 行可被抽为错误行；翻牌文案改为"额度/限流/过载"。此前 gemini 7 次调用 4 次 503、self 撞 529 都被记成 error/empty，下一会话照样推荐。回归：43a
- 2026-09-25 [C045 / 使用审计回流] `codev_model_of` 把裸 `default` 归一为 `<agent>-default`：账本里 codebuddy 同一默认模型被写成 codebuddy-default / default / unknown 三桶，`codev_stats` 每桶都不够 20 条样本。回归：43b
- 2026-09-25 [C046 / 使用审计回流] `codev_finding_add` / `codev_opinion_add` 新增 `codev_key_round`（`r12` 与 `12` 统一写 `12`，非数字拒收）与 `codev_key_agent`（agent 列只收小写标签，模型名/"实跑/推演"混入即拒收）；此前同一对象轮次写成 5..9 + r10..r14、agent 写成 "self claude-opus-5"（10 行），回放把同一主体当成两个人。写入实参个数与枚举校验不变。回归：43c/43d
- 2026-09-25 [C047 / 使用审计回流] `SKILL.md` 通用机制 A1 的默认组合按轮次分档：第 1 轮 codex + reasonix（+codebuddy/qoderclicn），第 N ≥ 2 轮 codex + self（+G1 check），reasonix/codebuddy 只在用户点名时加回，推荐语附账本数据（reasonix 33 次 ok 82 CNY、超时也计费、第 2 轮起多次 0 独家；codebuddy 56 次 6 次 1200-1800s 超时零输出）；brainstorm 推荐语注明 gemini 503 率与不钉 `-m gemini-2.5-pro`
- 2026-09-25 [C048 / 使用审计回流] `SKILL.md` Step 1 新增"轻量单家复审"（`--agents <一家>` 或口头"让 codex 看一眼"：不弹 A1/A2、不起 G1/G2、不做矩阵，保留扫描/翻牌/账本/逐字呈现/亲验）与"同一会话已确认的组合与分工模式沿用不再弹问"；依据：用户三次绕开 codev 自己跑 codex 贴回 127 行结果，单会话最多弹 12 次 A1
- 2026-09-25 [C049 / 使用审计回流] `SKILL.md` Step 0 新增 worktree 隔离会话的调用方式（Step 0 与后台调用写成 scratchpad 脚本文件、`CODEV_DIR` 仍 mktemp）与 zsh 三个新坑（`=` 开头裸词展开、前缀赋值不作用于下一行、glob 无匹配终止）；`bin/codev-lib.sh` source 时对缺随机后缀的会话目录名告警。依据：四个会话 19 次宿主拒绝、八个会话 15 次 `===== not found`、账本 4 行会话名 "codev"。回归：44c
- 2026-09-25 [C050 / 使用审计回流] `references/synthesis.md` §6：新增第 0 步 `codev_round_trend`（≥ 5 轮先和用户看范围）与"一个对象只用一个 slug"；收敛判据加"按章节收敛"（连续两轮只产 P3 的章节移出范围）；§6.3 停止条件 3 改为只有判断组成员额度耗尽才停、评审组一家失效而有效外部 ≥ 2 则继续，条件 5 区分"到达上限·末轮无 P1（待复核）"，并在停止后补一次只跑 G1 的末轮回流自查。依据：一份计划跨会话磨到第 14 轮、四个会话停在"到达上限但末轮 P1=0"、一次 qoderclicn 额度耗尽停了整个 `--auto`
- 2026-09-25 [C051 / 使用审计回流] `SKILL.md` 通用机制 G1 补 `check` 的调用账本收尾片段（此前发现台账 302 条 check 发现对应调用账本只有 15 行），G2 明确"先 Write 正文再 codev_report"并说明库守卫；通用机制 C 新增各 agent 预期用时（账本中位数）与 `⛔ 未启动` 的处理；Step 2F 第 1 步绑定 `SLUG` 并在全部四处沿用，第 6 步归档改在全部 report 完成之后
- 2026-09-25 [C052 / 使用审计回流] `references/agents.md`：新增 `pi` 条目（2026-09-15 起 14 次实测形态、弱只读级别、计量缺失、一次 6904s 未被安全网兜住）与只读风险表行，`codev_probe` 探测 pi；gemini 去掉 `-m gemini-2.5-pro` 建议并记录 503 死因；reasonix 记录 431KB 文档 bounded reader 失败的行号区间做法与成本数据；codebuddy 固定 `CODEV_MODEL_codebuddy=codebuddy-default` 与超时基线；函数表补 `codev_round_trend` / `codev_prompt_gate` / `codev_scan_triage` 与两道 report 守卫；失败处理表 quota 行覆盖过载。`README.md` / `references/prompts.md` 同步 pi 与新 FAQ。回归：44f

- 2026-09-11 [C005] `specs/2026-09-10-review-protocol-v2.md` 升至 v0.2，按用户确认将两人上限仅用于最终判断组，评审组允许多个 agent；同步选择、差异分工、自审及验收场景，文档一致性与差异检查通过，协议实现尚未开始
- 2026-09-11 [C007 / 评审回流 / cr-02] `specs/2026-09-10-review-protocol-v2.md` §6.1 明确记录类文件（changelog、发现台账、意见记录、任务状态、轮次归档）不进入三个快照的比对范围，并给出"日志本身是评审对象时重取 `verified_snapshot`"的例外；此前 §5 第 7 步在 `verified_snapshot` 之后写日志，codev 评审自身时 §9 的"最终产物与验证快照一致"永远无法满足。文本核对通过
- 2026-09-11 [C008 / 评审回流 / cr-03] 同文档 §3.4 增加 |J|=1 单判断主体条款（明确采纳即进执行清单、明确驳回即关闭、存疑或未返回不执行），并把原有条目限定为 |J|=2；此前所有条目按两位判断者书写，"另一人未返回"对单主体恒真，照章执行会让 §3.3 第 1、5 行的单主体任务无任何条目可执行。文本核对通过
- 2026-09-11 [C009 / 评审回流 / cr-04] 同文档 §9 把"对象缺陷、回流缺陷两列"改为引用 §8.1 的成因列（一列四值），并写明不拆成两个并列布尔列、不与 `reopened` 混列；此前的表述与 §8.1 的设计冲突，且在现有 13 列账本中没有对应列。文本核对通过
- 2026-09-11 [C010 / 评审回流 / cr-05] 同文档 §8.2 在"保留旧接口调用语义"与"可兼容扩展"之间做出选择：实施时用独立结构化事件文件，不改动 `codev_finding_add` 的实参个数与枚举校验；该函数的 `[ $# -eq 12 ]` 是刻意用 `-eq`、枚举按第 9/10/11 位校验，追加列会让每个既有调用点直接报用法错误。已核对库中的实参断言与 `codev_stats` 的取值字段
- 2026-09-11 [C011 / 评审回流 / cr-06] 同文档 §7 为取消固定 G2 后的 `self` 数据指定来源：需要「Claude vs 外部 agent」一致率时把 M 的 fresh-subagent 显式选入评审组（标签 `self`、角色 `评审`，不折算判断票），未选入则标"本轮无 self 样本"，并要求实施时一并改掉 `SKILL.md` 中以 G2 为唯一数据源的必做统计；此前取消固定 G2 会让该必做统计在空集上运行。文本核对通过
- 2026-09-11 [C012 / 评审回流] 同文档升至 v0.3，§12 增补 6 个验收场景（判断者修法分歧与缺席、单主体任务、未选入 `self`、日志在评审范围内、旧写入函数未被改动、提交文件清单含未绑定变量）并在实施顺序第 3 步纳入 §8.3。文本核对通过

### 🐛 Fixed

- 2026-09-25 [C053 / 使用审计回流] `bin/codev-lib.sh` 的 `codev_report`：`self`/`check` 正文文件为空但 `CODEV_TOKENS_<agent>` > 0 时判定漏落盘，不记账、返回 1 并提示先 Write 正文；此前三个会话六次把有报告的 G2 记成 empty，一次靠手工 awk 改账本。token 为 0 的真空回复仍照常归类。回归：43f
- 2026-09-25 [C054 / 使用审计回流] `codev_bg_sandboxed`：想铺 `./repo` 却退回空目录（超闸门 / 非 git）时写 `codev-degraded-<agent>` 标记，`codev_report` 在 ✔ 后追加"无代码视野"警告并在账本备注记 `退化:空目录`，`codev_prepare_call` 清掉上一轮标记；母本与已扫描版本不一致（`codev_repo_copy` 现返回 2）时**不启动**、打 `⛔ 未启动`、不写账本。此前 2026-09-15 一整轮 codebuddy + pi 在静默 text 模式下白跑、翻牌仍是 ✔。测试 36 的期望同步改为 rc=2。回归：43g/44b
- 2026-09-25 [C055 / 使用审计回流] `codev_err_lines` 的三位状态码行只认 4xx/5xx 且其后为文字：codex 回显到 stderr 的带行号源码（`354  void …`）不再被当错误行，此前七个会话 35 次 "✔ 但 stderr 含错误行" 全是假警告。回归：44a
- 2026-09-25 [C056 / 使用审计回流] 新增 `codev_round_trend <repo> <doc>`（各轮 发现/声称 P1/亲验成立 P1/agent，≥ 5 轮提示上限，兼容旧 `r` 前缀行）、`codev_prompt_gate`（按 agent 阈值检查全部提示词体积）、`codev_scan_triage`（secret 扫描命中按文件聚合 + 高置信形态单列，返回 3/0/1）；此前这三段每个会话各自手写一版。回归：43e/44d/44e

- 2026-09-11 [C013 / 修复 / cr-01] `SKILL.md` Step 2F 第 1 步补上 `CHANGELOG=<日志路径>` 绑定，第 6 步补上发出前的非空校验与提交后照 `暂存:` 行核对两个文件；此前第 6 步的 `codev_commit_round "$DOC $CHANGELOG"` 引用了全流程从未赋值的变量，空 token 被按空白拆分丢弃，只提交文档却照样打印「✔ 已提交第 N 轮回流」，日志改动静默留在工作区。已在一次性 git 仓库里复现（提交只含文档、`git status` 仍显示 `M CHANGELOG.md`）并核对修复后的文本；`TESTING.md` 真机清单加了对应勾选项
- 2026-09-11 [C014 / 修复 / replay-01] `bin/codev-lib.sh` 的意见回放保留完整追加历史，计票仅取同一范围内每个判断主体最后追加的立场，模型变化不新增票，三个判断主体报告选择冲突；回归覆盖同分钟、时间回退、改判后重新形成一致或分歧
- 2026-09-11 [C015 / 修复 / replay-02] 意见回放按仓库、对象、轮次、task 和问题编号隔离并支持对应筛选参数；写入仍严格接受 11 个实参，第 13 列追加 `CODEV_TASK_ID`（默认当前会话名，跨会话恢复需沿用），旧 12 列可读但缺任务归属时仅供回顾，不据此执行或关闭问题；同步使用说明并验证交错记录、跨任务同名编号和新旧混读
- 2026-09-11 [C016 / 修复 / replay-03] 回放独立比较有效判断的严重级别，同修法下的 P1/P2 或 P1/未知级别分歧仍保留阻塞，补证改判后重新判断；回归覆盖分歧保留及澄清解除
- 2026-09-11 [C017 / 修复 / replay-04] 回放将空白、`-`、`同意`、`待补充`、`TBD` 修法标为待补充，单主体或双主体均不得因占位符相同而进入执行清单，双方补齐后可重新判断；本批新增 23 项回归，bash/zsh 全量各 176 项通过，独立定向检查覆盖四项修复，完整文案 3+3 微测试及外部 agent 真机流程未运行
- 2026-09-11 [C018 / 自审修复 / lib-01] 工作区签名纳入未跟踪文件的路径及长度，初始仓库使用空树基线并禁用 textconv，上游读取及哈希失败不再生成可用签名；四种签名回归及既有母本并发测试通过
- 2026-09-11 [C019 / 自审修复 / lib-02] `codev_commit_round` 全程使用字面 Git 路径，避免通配符或 pathspec magic 扩大提交范围；字面通配文件提交、无匹配拒绝及既有索引恢复测试通过
- 2026-09-11 [C020 / 自审修复 / lib-03] `codev_archive` 逐文件检查复制状态，失败返回非零且不宣称归档成功，拒绝点路径 slug 与非数字轮次；复制失败、参数边界及 worktree 归档测试通过
- 2026-09-11 [C021 / 自审修复 / lib-04] native/sandboxed 调用统一准备输出，包含提前跳过路径，删除上一轮 metrics，准备失败不启动 CLI；重复调用不再把旧 token 和成本计入本轮，两个调用入口及跳过路径测试通过
- 2026-09-11 [C022 / 自审修复 / lib-05] 输出文件在独立 umask 下创建并显式设为 0600，JSON 解包替换也保留 0600；普通和结构化输出的权限测试通过
- 2026-09-11 [C023 / 自审修复 / lib-06] 结构化 `is_error` 与 `error*` subtype 优先于正文 PASS 和进程 rc=0，空错误结果保留失败提示，坏 usage 在修改正文前拒绝；最后补修确保坏 usage、metrics 写入和正文替换失败均不丢失已识别的错误状态，错误分类、原文保留及最终 bash/zsh 全量测试通过
- 2026-09-11 [C025 / 自审修复 / r2-01] 结构化结果状态以绑定正文 SHA-256 的 `codev-result-<agent>.json` 保留，重复 report 不再把失败变为成功，新调用清除旧状态；兼容前导空白及 result 对象，错误重放、成功解包和新旧输出隔离测试通过
- 2026-09-11 [C026 / 自审修复 / r2-02] metrics 改为解析完整 JSON 顶层字段，以 Python 整数和 Decimal 处理 token 与成本，拒绝缺失/坏类型的总计及无效成本覆盖值；大整数、科学计数法、嵌套字段和损坏 JSON 测试通过
- 2026-09-11 [C027 / 自审修复 / r2-03] 三个账本共用 Python 3 文件锁完整追加，保持既有列布局与写入实参数量，stdin 传递长字段并清洗 Tab/CR/LF，写入失败可见；并发短记录及 70KB 长备注字段一致性测试通过
- 2026-09-11 [C028 / 自审修复 / r2-04] 意见回放不再静默丢弃损坏行；发现布局、枚举或主体/问题 ID 损坏时返回非零并停止给出执行/关闭结论；坏行混入有效采纳记录的回归通过
- 2026-09-11 [C029 / 自审修复 / r2-05] 归档先完成复制再原子发布，保留已成功归档的历史，不同内容不可覆盖同轮次，相同内容可重复调用，结果状态一并保存；归档重复调用、覆盖拒绝与旧原文保留测试通过
- 2026-09-11 [C031 / 自审修复 / r3-01] `bin/codev-lib.sh` 先按 0600 原子保存计量与状态再替换正文，保存失败保留原始 JSON 并提示重试；同步 README 与接口说明，计量故障恢复、token/成本保留及权限回归通过
- 2026-09-11 [C032 / 自审修复 / r3-02] 结构化结果无法解析或保存时返回 12，由 report 标为 error；缺失/损坏 Python 不再把 is_error 与 PASS 的组合判为成功，普通方括号正文、已知错误与超时优先级回归通过
- 2026-09-11 [C033 / 自审修复 / r3-03] 三个账本的四个读取入口通过 `codev_tsv_snapshot` 在共享锁内取得私有副本，避免半行新判断导致旧一致放行，读取失败返回非零；同步 Python 和锁协议说明，暂停写者的意见回放及四个入口错误传播测试通过
- 2026-09-11 [C034 / 自审修复 / r3-04] `codev_sbox_gc` 按物理路径保护当前会话及父目录，定位失败时停止清理；同步接口说明，符号链接、尾斜杠、嵌套会话及无关过期目录清理回归通过
- 2026-09-11 [C035 / 自审修复 / r3-05] `codev_opinion_add` 与回放统一拒绝空白 agent/issue，防止接口自身写入无效身份；四种空值/纯空格拒收回归通过，`TESTING.md` 补充本轮验证范围
- 2026-09-11 [C038 / 自审修复 / r4-01] `bin/codev-lib.sh` 的 `codev_commit_round` 拒收含空白占位的文件清单（开头/结尾空白或连续两个空白），并回显原串点名；此前 `"$DOC $CHANGELOG"` 少绑变量会展开成空 token 被静默丢弃，只提交剩下的文件却返回 0 并打印「✔ 已提交第 N 轮回流」，C013 只在 `SKILL.md` 层面处理、库侧原样敞开。三种空位形状各用独立仓库验证被拒且不产生提交，单文件/多文件/换行分隔三种合法形状不受影响
- 2026-09-11 [C039 / 自审修复 / r4-02] `codev_finding_add` 与 `codev_opinion_add` 在账本写入失败时各自输出「未记入台账/未记入记录」并带关键字段，与 `codev_ledger_append` 对齐；此前只返回 1 而不作声，批量逐条记录时这条发现会与写成功的混在一起静默消失。用父路径被普通文件占住构造真实写入失败验证
- 2026-09-11 [C040 / 自审修复 / r4-03] `codev_bg_native` 把 ▶ 启动行移到"无 timeout 则跳过"判断之后，与 `codev_bg_sandboxed` 一致；此前先 ▶ 后 ⏭ 会让没启动的 agent 在运行时状态板上挂成运行中。两个入口的跳过路径均只输出 ⏭
- 2026-09-11 [C041 / 自审修复 / r4-04] `tests/test-lib.sh` 的 `bad` 补 `return 0`；此前它以 `[ -n "${2:-}" ] &&` 结尾，单实参调用返回 1，使遍布全文件的 `cmd && bad X || ok Y` 继续执行 `ok`，同一条断言既记失败又记通过、`pass` 数虚增（实测审前版本每条 ✘ 都配一个假 ✔）。修复后矛盾行消失，计数由 241/9 更正为 238/9
- 2026-09-11 [C042 / 评审修复 / r4-01 补修] `bin/codev-lib.sh` 的文件清单守卫直接替换制表符并保留全部末尾换行，修复 C038 中命令替换剥掉换行导致空文件项仍被放行的问题；`tests/test-lib.sh` 改用真实换行夹具，验证连续末尾换行拒收且 HEAD/暂存区不变，以及合法清单完整提交，`TESTING.md` 同步覆盖范围；修复前相关用例 6 项失败，修复后 bash/zsh 全量各 255 项通过
